<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ResivirDatos" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/orange_primary</item>
        <item name="colorSecondary">@color/orange_secondary</item>
        <item name="colorAccent">@color/orange_accent</item>
        <item name="android:windowBackground">@color/orange_background</item>
    </style>

    <style name="Theme.ResivirDatos" parent="Base.Theme.ResivirDatos" />
</resources>